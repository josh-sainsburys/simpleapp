apiVersion: v1
kind: Service
metadata:
  name: label-demo
  labels:
    environment: '${ parameters.environment }'
    app: nginx
spec:
  ports:
    containers:
    - name: nginx
      image: nginx:1.7.9
      ports:
      - containerPort: 80